-- [[ MM2 XENO ULTIMATE - VERSION FIXED ]] --
-- Rimosse tutte le library esterne per evitare crash all'esecuzione

local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local RunService = game:GetService("RunService")
local RepStorage = game:GetService("ReplicatedStorage")
local UIS = game:GetService("UserInputService")

-- CREAZIONE GUI COMPLETAMENTE LOCALE (Senza loadstring)
local ScreenGui = Instance.new("ScreenGui")
local MainFrame = Instance.new("Frame")
local SideBar = Instance.new("Frame")
local Title = Instance.new("TextLabel")
local ContentFrame = Instance.new("Frame")
local UIListLayout = Instance.new("UIListLayout")

-- Gestione sicura della GUI per Xeno
pcall(function()
    ScreenGui.Parent = game:GetService("CoreGui")
end)
if not ScreenGui.Parent then
    ScreenGui.Parent = LocalPlayer:WaitForChild("PlayerGui")
end

ScreenGui.Name = "XenoHub_MM2"

MainFrame.Name = "MainFrame"
MainFrame.Parent = ScreenGui
MainFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 30)
MainFrame.Position = UDim2.new(0.3, 0, 0.3, 0)
MainFrame.Size = UDim2.new(0, 450, 0, 300)
MainFrame.BorderSizePixel = 0
Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 12)

SideBar.Name = "SideBar"
SideBar.Parent = MainFrame
SideBar.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
SideBar.Size = UDim2.new(0, 130, 1, 0)
SideBar.BorderSizePixel = 0
Instance.new("UICorner", SideBar).CornerRadius = UDim.new(0, 12)

Title.Name = "Title"
Title.Parent = SideBar
Title.Text = "XENO HUB"
Title.TextColor3 = Color3.fromRGB(0, 200, 255)
Title.TextSize = 20
Title.Font = Enum.Font.GothamBold
Title.Size = UDim2.new(1, 0, 0, 50)
Title.BackgroundTransparency = 1

ContentFrame.Name = "ContentFrame"
ContentFrame.Parent = MainFrame
ContentFrame.Position = UDim2.new(0, 140, 0, 10)
ContentFrame.Size = UDim2.new(1, -150, 1, -20)
ContentFrame.BackgroundTransparency = 1

UIListLayout.Parent = ContentFrame
UIListLayout.Padding = UDim.new(0, 8)

-- =========================================================
-- X ROSSA PER CHIUDERE LA GUI
-- =========================================================

local CloseButton = Instance.new("TextButton")
CloseButton.Name = "CloseButton"
CloseButton.Parent = MainFrame
CloseButton.BackgroundColor3 = Color3.fromRGB(220, 40, 40)
CloseButton.Position = UDim2.new(1, -38, 0, 8)
CloseButton.Size = UDim2.new(0, 30, 0, 30)
CloseButton.Text = "X"
CloseButton.TextColor3 = Color3.fromRGB(255, 255, 255)
CloseButton.TextSize = 18
CloseButton.Font = Enum.Font.GothamBold
CloseButton.BorderSizePixel = 0
CloseButton.ZIndex = 10

Instance.new("UICorner", CloseButton).CornerRadius = UDim.new(0, 8)

-- =========================================================
-- ICONA DRAGO IN ALTO AL CENTRO
-- Sostituisci 0 con l'ID della tua immagine Roblox
-- =========================================================

local DragonButton = Instance.new("ImageButton")
DragonButton.Name = "DragonButton"
DragonButton.Parent = ScreenGui
DragonButton.BackgroundColor3 = Color3.fromRGB(20, 20, 30)
DragonButton.BackgroundTransparency = 0
DragonButton.Position = UDim2.new(0.5, -35, 0, 15)
DragonButton.Size = UDim2.new(0, 70, 0, 70)
DragonButton.Image = "rbxassetid://0"
DragonButton.ScaleType = Enum.ScaleType.Crop
DragonButton.Visible = false
DragonButton.ZIndex = 20
DragonButton.BorderSizePixel = 0

Instance.new("UICorner", DragonButton).CornerRadius = UDim.new(1, 0)

local DragonStroke = Instance.new("UIStroke")
DragonStroke.Parent = DragonButton
DragonStroke.Color = Color3.fromRGB(0, 200, 255)
DragonStroke.Thickness = 2

-- CHIUDE LA GUI CON LA X
CloseButton.MouseButton1Click:Connect(function()
    MainFrame.Visible = false
    DragonButton.Visible = true
end)

-- RIAPRE LA GUI CLICCANDO SUL DRAGO
DragonButton.MouseButton1Click:Connect(function()
    MainFrame.Visible = true
    DragonButton.Visible = false
end)

-- SETTINGS
local Settings = {
    KillAura = false,
    EspEnabled = false,
    WalkSpeed = 16,
    AuraRange = 20
}

local TargetRemote = nil

local function ScanForRemotes()
    for _, v in pairs(RepStorage:GetDescendants()) do
        if v:IsA("RemoteEvent") and (v.Name:lower():find("knife") or v.Name:lower():find("attack") or v.Name:lower():find("stab")) then
            TargetRemote = v
            break
        end
    end
end

-- FUNZIONE PER CREARE BOTTONI
local function CreateToggle(text, default, callback)
    local Btn = Instance.new("TextButton")
    Btn.Size = UDim2.new(1, 0, 0, 40)
    Btn.BackgroundColor3 = Color3.fromRGB(40, 40, 55)
    Btn.Text = text .. ": " .. (default and "ON" or "OFF")
    Btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    Btn.Font = Enum.Font.Gotham
    Btn.Parent = ContentFrame
    Instance.new("UICorner", Btn).CornerRadius = UDim.new(0, 6)

    local state = default
    Btn.MouseButton1Click:Connect(function()
        state = not state
        Btn.Text = text .. ": " .. (state and "ON" or "OFF")
        Btn.BackgroundColor3 = state and Color3.fromRGB(0, 150, 255) or Color3.fromRGB(40, 40, 55)
        callback(state)
    end)
end

-- AGGIUNTA FUNZIONI
CreateToggle("Kill Aura", false, function(val) 
    Settings.KillAura = val 
    if val then ScanForRemotes() end
end)

CreateToggle("ESP Wallhack", false, function(val) 
    Settings.EspEnabled = val 
end)

-- SPEED CONTROL
local SpeedBtn = Instance.new("TextButton")
SpeedBtn.Size = UDim2.new(1, 0, 0, 40)
SpeedBtn.BackgroundColor3 = Color3.fromRGB(40, 40, 55)
SpeedBtn.Text = "Speed: " .. Settings.WalkSpeed
SpeedBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
SpeedBtn.Font = Enum.Font.Gotham
SpeedBtn.Parent = ContentFrame
Instance.new("UICorner", SpeedBtn).CornerRadius = UDim.new(0, 6)

SpeedBtn.MouseButton1Click:Connect(function()
    Settings.WalkSpeed = Settings.WalkSpeed + 5
    if Settings.WalkSpeed > 100 then Settings.WalkSpeed = 16 end
    SpeedBtn.Text = "Speed: " .. Settings.WalkSpeed
end)

-- LOOP LOGIC (Heartbeat)
RunService.Heartbeat:Connect(function()
    if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Humanoid") then
        LocalPlayer.Character.Humanoid.WalkSpeed = Settings.WalkSpeed
    end

    if Settings.KillAura then
        if LocalPlayer.Character and (LocalPlayer.Character:FindFirstChild("Knife") or LocalPlayer:GetAttribute("Role") == "Murderer") then
            if not TargetRemote then ScanForRemotes() end
            local myRoot = LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
            if myRoot then
                for _, p in pairs(Players:GetPlayers()) do
                    if p ~= LocalPlayer and p.Character and p.Character:FindFirstChild("HumanoidRootPart") then
                        if (myRoot.Position - p.Character.HumanoidRootPart.Position).Magnitude <= Settings.AuraRange then
                            if TargetRemote then TargetRemote:FireServer(p) end
                        end
                    end
                end
            end
        end
    end
end)

-- LOOP ESP (RenderStepped)
RunService.RenderStepped:Connect(function()
    for _, p in pairs(Players:GetPlayers()) do
        if p ~= LocalPlayer and p.Character then
            local hl = p.Character:FindFirstChild("XenoESP") or Instance.new("Highlight", p.Character)
            hl.Name = "XenoESP"
            hl.Enabled = Settings.EspEnabled
            hl.FillColor = (p:GetAttribute("Role") == "Murderer") and Color3.fromRGB(255, 0, 0) or Color3.fromRGB(255, 255, 255)
        end
    end
end)

-- DRAGGING SYSTEM (Semplificato e ultra-compatibile)
local dragging, dragInput, dragStart, startPos

MainFrame.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        dragging = true
        dragStart = input.Position
        startPos = MainFrame.Position
    end
end)

UIS.InputChanged:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement and dragging then
        local delta = input.Position - dragStart
        MainFrame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
    end
end)

UIS.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        dragging = false
    end
end)

print("Xeno Hub Loaded Successfully!")
